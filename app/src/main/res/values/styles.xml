<resources>

    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/magenta_bright</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/logo_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.App.Main</item>
    </style>

    <!-- App Theme -->

    <style name="Theme.App.Main" parent="Theme.AppCompat.NoActionBar">

        <item name="android:windowBackground">@android:color/black</item>

        <!-- disable all caps -->

        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>

        <!-- enable fullscreen mode -->

        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- set color accent -->

        <item name="colorAccent">@color/accentuated_tv</item>

    </style>

    <style name="Shadow">
        <item name="android:shadowColor">@android:color/black</item>
        <item name="android:shadowDx">5</item>
        <item name="android:shadowDy">5</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="ItalicText">
        <item name="android:textStyle">italic</item>
    </style>

    <!--    App Title TextView-->

    <style name="AutoCropTV" parent="Shadow">
        <item name="android:text">@string/app_title</item>
        <item name="android:textColor">@color/magenta_saturated</item>
        <item name="android:fontFamily">@font/aadhunik</item>
    </style>

    <style name="AutoCropTVBig" parent="AutoCropTV">
        <item name="android:textSize">52sp</item>
    </style>

</resources>